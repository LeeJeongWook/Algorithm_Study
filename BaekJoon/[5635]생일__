#define _CRT_SECURE_NO_DEPRECATE
#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

int month[12] = { 31, 28, 31, 30, 31, 30, 31, 31, 30, 31, 30, 31 };

int main() {
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	freopen("input.txt", "r", stdin);
	
	int studentCnt;
	
	cin >> studentCnt;

	string tmp;
	vector<pair<int, string>> std;

	for (int student = 0; student < studentCnt; student++) {
		int sum = 0;
		string str;
		for (int i = 0; i < 4; i++) {
			cin >> tmp;
						
			if (i == 0) {
				str = tmp;
			}
			
			if (i == 1) {
				sum += stoi(tmp);
			}
			else if (i == 2) {
				sum += month[stoi(tmp) - 1];
			}
			else if (i == 3) {
				sum += stoi(tmp) * 365;
			}
		}
		std.push_back(make_pair(sum, str));
	}

	sort(std.begin(), std.end());

	cout << std[std.size() - 1].second << endl;
	cout << std[0].second << endl;



	return 0;
}
