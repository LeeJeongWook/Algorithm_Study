#define _CRT_SECURE_NO_WARNINGS
//#include <stdio.h>
#include <iostream>
#include <vector>

using namespace std;

int main() {
	int N, tmp;
	
	freopen("input.txt", "r", stdin);

	cin >> N;

	vector<int> v(N, 0);
	vector<vector<int>> field(N, vector<int>(N, 0));

	for (int i = 0; i < N; i++) {
		for (int j = 0; j < N; j++) {
			cin >> tmp;
			field[i][j] = tmp;
		}
	}

	for (int i = 0; i < N; i++) {
		for (int j = 0; j < N; j++) {
			cout << field[i][j];
		}
		cout << endl;
	}

	return 0;
}
