#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <stdio.h>

using namespace std;

int main() {
    int A, B;

    cin >> A >> B;

    for (int i = 0; i < B; i++) {
        if ((B % i) == 0) {
            cout << i << " ";
        }
        //cout << i << " ";
    }

    return 0;
}

/*
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <stdio.h>
#include <cmath>

using namespace std;

int gcdf(int a, int b)
{
    int c;
    while (b)
    {
        c = a % b;
        a = b;
        b = c;
    }
    return a;
}

int main()
{
    int gcd, lcm, tmp, ans1, ans2;

    scanf("%d %d", &gcd, &lcm);
    ans1 = ans2 = gcd;
    lcm /= gcd;

    for (int i = sqrt(lcm); i >= 1; i--)
    {
        if (lcm % i == 0 && gcdf(i, lcm / i) == 1)
        {
            ans1 *= i;
            ans2 *= (lcm / i);
            break;
        }
    }

    if (ans1 > ans2)
    {
        tmp = ans1;
        ans1 = ans2;
        ans2 = tmp;
    }

    printf("%d %d", ans1, ans2);
    return 0;
}
*/
//
