#include <iostream>

using namespace std;

#define maxVal 100

void main() {
	cin.tie(NULL);
	// ios::sync_with_stdio(false);

	int m = 3, n = 16;

	cin >> m >> n;

	int num[maxVal] = {};

	for (int i = 2; i <= n; i++) {
		num[i] = i;
	}

	for (int i = 2; i <= n; i++) {
		if (num[i] == 0) continue;
		for (int j = 2 * i; j <= n; j += i) {
			num[j] = 0;
		}
	}

	for (int i = m; i <= n; i++) {
		if (num[i] != 0) cout << num[i] << "\n";
	}
}
